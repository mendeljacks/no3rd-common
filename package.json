{
    "name": "no3rd-common",
    "version": "1.0.22",
    "description": "Business logic and utilities for use accross no3rd projects",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prepare": "husky install",
        "bump": "npm version patch --no-git-tag-version && git add package*.json && npm publish"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "husky": "^7.0.4"
    },
    "dependencies": {
        "buffer": "^6.0.3",
        "dayjs": "^1.11.0",
        "walmart_marketplace": "^1.0.11",
        "yay_json": "^1.0.19"
    }
}
